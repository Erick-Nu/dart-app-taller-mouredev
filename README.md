# Dart App - Taller MoureDev

Este repositorio contiene una colección de ejercicios prácticos diseñados para aprender los fundamentos del lenguaje de programación **Dart**. Los ejercicios siguen una progresión didáctica, cubriendo desde la sintaxis básica hasta la Programación Orientada a Objetos, basados en el taller de MoureDev.

## 📂 Contenido del Repositorio

Los archivos están numerados secuencialmente según la complejidad de los temas:

- **[onePractice.dart](onePractice.dart)**: Introducción al lenguaje.
  - Función `main`.
  - Impresión en consola (`print`).
  - Sintaxis de comentarios (una y varias líneas).

- **[twoPractice.dart](twoPractice.dart)**: Variables y Tipos de Datos.
  - Declaración de variables (`var`, `String`, `int`, `double`, `bool`).
  - Mutabilidad vs Inmutabilidad (`final`, `const`).
  - Reasignación de valores.

- **[threePractice.dart](threePractice.dart)**: Control de Flujo (Condicionales).
  - Estructuras `if`, `else if`, `else`.
  - Operadores lógicos (`&&`, `||`) y de comparación.

- **[fourPractice.dart](fourPractice.dart)**: Funciones y Colecciones.
  - Declaración de funciones con y sin retorno.
  - **Listas**: Arrays ordenados.
  - **Sets**: Colecciones de elementos únicos.
  - **Mapas**: Estructuras clave-valor.

- **[fivePractice.dart](fivePractice.dart)**: Bucles y Ciclos.
  - Bucle `for` para iterar listas.
  - Bucle `while` con contadores.

- **[sixPractice.dart](sixPractice.dart)**: Programación Orientada a Objetos (POO).
  - Definición de clases (`class`).
  - Atributos y Constructores.
  - Instanciación de objetos.

- **[sevenPractice.dart](sevenPractice.dart)**: Tipos Enumerados.
  - Uso de `enum` para definir constantes con nombre (ej. lenguajes de programación).

## 🚀 Cómo ejecutar los archivos

Para correr cualquiera de los scripts, asegúrate de tener el SDK de Dart instalado y ejecuta el siguiente comando en tu terminal:

```bash
dart run <nombre_del_archivo>.dart